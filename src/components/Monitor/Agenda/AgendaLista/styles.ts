import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  container: {
    width: '100%',
    height: '85%',
    alignItems: 'center',
    marginTop: -30
  },
  meetingCard: {
    backgroundColor: '#f2f2f2', 
    width: '100%',
    marginBottom: 20,
    padding: 20,
    borderRadius: 5,
  }
})